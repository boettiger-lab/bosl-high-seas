apiVersion: apps/v1
kind: Deployment
metadata:
  name: bosl-high-seas
  labels:
    app: bosl-high-seas
spec:
  replicas: 1
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1
  selector:
    matchLabels:
      app: bosl-high-seas
  template:
    metadata:
      labels:
        app: bosl-high-seas
    spec:
      initContainers:
      - name: copy-content
        image: busybox:1.37
        command:
        - sh
        - -c
        - |
          cp /content/index.html /usr/share/nginx/html/
          cp /content/layers-input.json /usr/share/nginx/html/
          cp /content/system-prompt.md /usr/share/nginx/html/
        volumeMounts:
        - name: website-content
          mountPath: /usr/share/nginx/html
        - name: app-content
          mountPath: /content
      containers:
      - name: nginx
        image: nginx:alpine
        ports:
        - containerPort: 80
        env:
        - name: TMPDIR
          value: "/tmp"
        - name: MCP_SERVER_URL
          value: "https://duckdb-mcp.nrp-nautilus.io/mcp"
        - name: PROXY_KEY
          valueFrom:
            secretKeyRef:
              name: open-llm-proxy-secrets
              key: proxy-key
              optional: true
        command:
        - sh
        - -c
        - |
          apk add --no-cache gettext
          # Client-side config: no LLM secrets (MCP_SERVER_URL only).
          envsubst '$${MCP_SERVER_URL}' \
            < /config/config.template.json \
            > /usr/share/nginx/html/config.json
          # Server-side nginx config: substitutes ${PROXY_KEY} into the
          # /api/llm/ reverse proxy's Authorization header. Limited to
          # ${PROXY_KEY} so nginx's own $uri / $remote_addr variables
          # are preserved.
          envsubst '$${PROXY_KEY}' < /nginx-template/nginx.conf.template \
            > /etc/nginx/conf.d/default.conf
          nginx -g 'daemon off;'
        resources:
          requests:
            cpu: "100m"
            memory: "128Mi"
          limits:
            cpu: "500m"
            memory: "256Mi"
        volumeMounts:
        - name: website-content
          mountPath: /usr/share/nginx/html
        - name: nginx-template
          mountPath: /nginx-template
          readOnly: true
        - name: nginx-conf-d
          mountPath: /etc/nginx/conf.d
        - name: config-template
          mountPath: /config
        livenessProbe:
          httpGet:
            path: /health
            port: 80
          initialDelaySeconds: 10
          periodSeconds: 30
        readinessProbe:
          httpGet:
            path: /health
            port: 80
          initialDelaySeconds: 5
          periodSeconds: 10
      volumes:
      - name: website-content
        emptyDir: {}
      - name: app-content
        configMap:
          name: bosl-high-seas-content
      - name: config-template
        configMap:
          name: bosl-high-seas-config
      - name: nginx-template
        configMap:
          name: bosl-high-seas-nginx
      - name: nginx-conf-d
        emptyDir: {}
